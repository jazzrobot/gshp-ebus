# Version Notes

## Planned milestones

- `v0.1` - raw byte capture, timestamps, and error counters
- `v0.2` - basic frame parsing and log clean-up
- `v0.3` - first validated signal extraction for Phase 1 close-out

## Current status

- `v0.1` is now scaffolded for the Waveshare `ESP32-S3-ETH` in [current/](current/README.md)
- current wiring assumption: `GPIO16` receive input, `3V3` on pin `36`, `GND` on pin `38`
